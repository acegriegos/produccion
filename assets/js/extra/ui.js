import './ui/alerts.js'

// const link = document.createElement('link');
// link.rel = 'stylesheet';
// // Usamos la ruta absoluta desde el dominio actual
// link.href = window.location.origin + '/assets/css/ui/alerts.css'; 

// document.head.appendChild(link);